<!--bootstrapに対応するようにフォームを修正する必要がある-->
<%= render "devise/shared/error_messages", resource: resource %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "layouts/labels" %>

        <div class="form-group row">
          <%= f.label :name, "名前", class: "col-2 col-form-label text-light" %><br>
          <div class="col-10">
            <%= f.text_field :name, class: "form-control shadow rounded", placeholder: "名前を入力してください", autofocus: true %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :email, "メールアドレス", class: "col-2 col-form-label text-light" %>
          <div class="col-10">
            <%= f.email_field :email, class: "form-control shadow rounded", placeholder: "メールアドレスを入力してください" %>
          </div>
        </div>
        <%# if @minimum_password_length %>
<!--          <p class="form-group row">-->
<!--            <em class="col-10">-->
<!--              password is <%#= @minimum_password_length %> characters minimum-->
<!--            </em>-->
<!--          </p>-->
        <%# end %>


        <div class="form-group row">
          <%= f.label :password, "パスワード", class: "col-2 col-form-label text-light" %>
          <div class="col-10">
            <%= f.password_field :password, class: "form-control shadow rounded", placeholder: "6文字以上でお願いします" %>
          </div>
        </div>

        <div class="form-group row ">
          <%= f.label :password_confirmation,"パスワード確認", class: "col-2 col-form-label text-light" %><br/>
          <div class="col-10 password-form password-confirmation">
            <%= f.password_field :password_confirmation, class: "form-control shadow rounded", autocomplete: "new-password", placeholder: "パスワードをもう一度入力してください" %>
          </div>
        </div>

        <div class="actions">
          <%= f.submit('サインイン', class: 'btn btn-lg btn-primary btn-block') %>
        </div>
      <% end %>
      <div class="link">
        <%= render "devise/shared/links" %>
      </div>
    </div>

  </div>

</div>

