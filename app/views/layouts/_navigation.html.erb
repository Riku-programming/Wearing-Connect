<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <%= link_to image_tag('', class: 'navbar-logo'), root_path, class: 'navbar-brand' %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%# if user_signed_in? && current_user.admin?  %>
<!--        <li class="nav-item">-->
          <%#= header_link_item(icon('fas', 'users') + ' すべてのユーザー', users_path) %>
<!--        </li>-->
      <%# end %>
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= header_link_item(icon('fas', 'rss') + 'アイテム一覧', items_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item(icon('fas', 'search') + ' アイテム検索', my_items_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item(icon('fa', 'user-plus') + '友達', follows_user_path(current_user.id)) %>
        </li>
      <% end %>
      <li class="nav-item">
        <%= header_link_item(icon('fa', 'heart') + 'お気に入り', like_user_path(current_user.id)) %>
      </li>
      <li class="nav-item">
        <%= header_link_item(icon('fas', 'tshirt') + 'コーディネート', '#') %>
      </li>
    </ul>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= header_link_item(icon('fas', 'edit') + ' 新しいアイテム', new_item_path) %>
        </li>
        <li class="nav-item dropdown">
          <%= link_to icon('fas', 'user') + " #{current_user.name}さん", '#', class: "nav-link dropdown-toggle", id: "navbarDropdown", role: "button", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" } %>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to icon('fas', 'user') + ' マイページ', user_path(current_user), class: 'dropdown-item' %>
            <%= link_to icon('fas', 'sign-out-alt') + ' ログアウト', destroy_user_session_path, class: 'dropdown-item', method: :delete, data: {confirm: "ログアウトしますか？"} %>
            <%= link_to icon('fas', 'edit') + '編集', edit_user_registration_path, class: 'dropdown-item', data: {confirm: 'ユーザー情報を編集しますか？'} %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= header_link_item(icon('fas', 'user-plus') + ' 新規登録', new_user_registration_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item(icon('fas', 'sign-in-alt') + ' ログイン', new_user_session_path) %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>